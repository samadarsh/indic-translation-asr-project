version: '3.8'

services:
  asr-system:
    build: .
    ports:
      - "7860:7860"
    volumes:
      - ./outputs:/app/outputs
      - ./sample_inputs:/app/sample_inputs
    environment:
      - GRADIO_SERVER_NAME=0.0.0.0
